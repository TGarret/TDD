def echo (a)
  "#{a}"
end

def shout (a)
  "#{a.upcase}"
end

def repeat (a,b=(boucle=true;nil))
  if boucle
    "#{a} #{a}"
  else
    "#{a} " * (b-1) + "#{a}"
end
end
def first_word (beg)
  array = beg.split(' ')
  array = 0
end
